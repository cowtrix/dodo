import { connect } from 'react-redux'
import { Role } from './role'

const mapStateToProps = state => ({

})

const mapDispatchToProps = dispatch => ({

})

export const RoleConnected = connect(mapStateToProps, mapDispatchToProps)(Role)
