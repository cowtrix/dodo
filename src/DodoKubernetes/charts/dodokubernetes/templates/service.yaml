apiVersion: v1
kind: Service
metadata:
  name: {{ template "dodokubernetes.fullname" . }}
  labels:
    app: {{ template "dodokubernetes.name" . }}
    chart: {{ template "dodokubernetes.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dodokubernetes.name" . }}
    release: {{ .Release.Name }}
